import React from "react";
import "./Resume.scss";

const resume = props => {
  return (
    <section id="resume">
      <h2 className="page__title">Resume</h2>
    </section>
  );
};

export default resume;
